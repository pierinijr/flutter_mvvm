import 'package:flutter/material.dart';
import 'package:flutter_mvvm/view/screens/page_home.dart';

final Map<String, WidgetBuilder> routes = {
  // "/": (context) => const PageSplash(),
  "/home": (context) => const PageHome(),
};
